[project]
name = "iac-demo"
version = "0.1.0"
description = "Infrastructure as Code with AWS CDK"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "aws-cdk-lib>=2.100.0",
    "constructs>=10.0.0,<11.0.0",
]

[tool.uv]
dev-dependencies = []

