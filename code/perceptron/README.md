# Perceptron

Binary classifier using the identity activation function.

## Files

| File | Description |
| --- | --- |
| `perceptron_model.py` | Perceptron implementation |
| `test_perceptron.py` | Unit tests on iris dataset |

## Environment

```sh
cd code && uv sync && uv run pytest perceptron/test_perceptron.py
```
